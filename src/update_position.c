#include "params.h"
#include "atoms.h"

update_position( Atoms * myatoms, misc_params * m_pars )
{

   // Update particle positions with Verlet algorithm
   int atomi;
   for ( atomi = 0; atomi < myatoms->N; atomi++ )
   {
   }

}